# Decision Log (Lab Notebook)

Every non-trivial scientific or engineering choice, one line each, with a rationale. This is the
basis for the write-up and defense. ⚕ = microbiology sign-off required.

| # | Date | Phase | Decision | Rationale | Owner |
|---|---|---|---|---|---|
| 1 | 2026-07-07 | 0 | Scaffold repo per CLAUDE.md §5; brief → `CLAUDE.md`, docx+diagrams → `proposal/`. | Reproducible skeleton before any science. | SWE |
| 2 | 2026-07-07 | 0 | Fixed global seed = 42 in `config/config.yaml`. | Reproducibility mandate. | SWE |
| 3 | 2026-07-07 | 0 | Both AMRFinderPlus + mlst in env; RGI commented as alternative. | Keep env ready; annotator chosen in Phase 4. | SWE |
| 4 | 2026-07-07 | 0 | Dropped `bvbrc-cli` pip dep; use BV-BRC REST Data API via `requests`. | No maintained pip CLI package; API is sufficient and lighter. | SWE |
| 5 | 2026-07-07 | 0 | Env built natively on osx-arm64 (no Rosetta); bio tools have arm64 builds. | Confirmed amrfinderplus 4.2.7 + mlst 2.33.1 available for osx-arm64. | SWE |
| 6 | 2026-07-07 | 1 | Survey via BV-BRC `genome_amr`; counts by antibiotic × phenotype using facet + Content-Range. | Exact counts with no bulk download; metadata only. | SWE |
| 7 | 2026-07-07 | 1 | ~~min(R,S)≥100, balance≥0.20~~ → **superseded by #9**. | Initial guess before data. | SWE |
| 8 | 2026-07-07 | 1 | Count **lab phenotypes only** (`evidence="Laboratory Method"`); exclude computational predictions. | Using BV-BRC computational AMR calls as labels would be circular. E.coli: 6.3M rows → 243K lab. | SWE |
| 9 | 2026-07-07 | 1 | **Tuned thresholds from live data: min(R,S)≥150, balance≥0.30, total(R+S)≥300.** | Data abundant (median min(R,S)=191); balance is the binding quality lever (median 0.15). Yields 51 balanced drugs / 7 orgs. | SWE (⚕ review) |
| 10 | 2026-07-07 | 1 | genome_amr has no `taxon_lineage_ids`; filter by species `taxon_id`. | Verified against live API schema. | SWE |
| 11 | 2026-07-07 | 1 | **LOCKED: organism = _Klebsiella pneumoniae_ (taxon 573).** | Best fit to thesis: carbapenem resistance is where single-gene rules fail (porin+β-lactamase combos → catchable VMEs); strong MLST clonality makes the phylogeny-aware split bite. | SWE + ⚕ |
| 12 | 2026-07-07 | 1 | **LOCKED: 5-drug panel** = meropenem, gentamicin, ciprofloxacin, TMP-SMX, cefoxitin. | 5 distinct determinant families → convincing per-drug SHAP; all clear thresholds. | SWE + ⚕ |
| 13 | 2026-07-07 | 1 | **Thin-slice drug = ciprofloxacin** (meropenem = Week-2 flagship). | Chromosomal gyrA/parC point mutations: clean direct signal, lineage-spread, exercises point-mutation feature path → de-risks Week-1 go/no-go. | SWE + ⚕ |
| 14 | 2026-07-07 | 2 | **Download A (phased): thin-slice balanced 750 R / 750 S cipro; run our own AMRFinderPlus.** | Under ceiling; proves pipeline before the 12 GB pull. Reproducible determinant vocabulary. | SWE + ⚕ |
| 15 | 2026-07-07 | 2 | **BUG CAUGHT + FIXED: genome_sequence API defaults to limit(25) → truncated assemblies (~2 Mbp of ~5.5).** Add `limit(25000)` + a <2.5 MB size guard; re-downloaded all. | Truncated genomes would drop genes on missing contigs → false-absent features → corrupted labels. Caught by the FASTA-size integrity check. | SWE |
| 16 | 2026-07-07 | 2 | Downloader: retry 4x w/ backoff on network errors; resumable. | A single ReadTimeout aborted the run at genome 306. | SWE |
| 17 | 2026-07-07 | 3 | **QC thresholds validated against real data (not guessed): completeness ≥95%, contamination ≤5%, contigs ≤500, length 4.8–6.5 Mbp.** Config units corrected fractions→CheckM %. | Data is 99% "Good"; moderate tier drops ~31 (2%), **class-balanced** (16R/15S → no bias). Stricter QC costs lineage diversity → hurts the phylogeny-aware split. | SWE (⚕ confirm) |
| 18 | 2026-07-07 | 3 | Analysis-code bug fixed: `float(v or default)` turned contamination=0 into 99 (falsy-zero) → falsely dropped clean genomes. | Real per-filter drops are tiny (3/14/23/7), not ~970. | SWE |
| 19 | 2026-07-07 | 3 | **⚕ APPROVED + literature-refined QC: completeness ≥90%, contamination ≤5% (MIMAG, Bowers 2017), contigs ≤500, length 4.5–7.5 Mbp (K. pneumoniae QC range).** Widened length cap 6.5→7.5. | Co-builder: keep thresholds but don't lose many samples + cite refs. Lit shows MDR/plasmid K. pneumoniae reach ~6.6 Mbp — a tight cap would drop real resistant strains. Now drops 28 (1.9%), recovers 3 clean genomes; contamination is the real filter. | SWE + ⚕ |
| 20 | 2026-07-07 | 4 | Features = AMRFinderPlus `--organism Klebsiella_pneumoniae`, Type==AMR Element symbols (acquired genes + point mutations); VIRULENCE/STRESS excluded. | Enables gyrA/parC point mutations (key cipro determinants); interpretable determinant vocabulary per proposal. | SWE |
| 21 | 2026-07-07 | 4 | Full 1472-genome annotation run overnight (~15–19h), resumable. | User chose full thin-slice over subset; ~47s/genome measured. | SWE + user |
| 23 | 2026-07-08 | 7 | **Full panel modeled by REUSING the 1472 annotated genomes** (multi-drug labels); no new download/annotation. | Each annotated genome carries labels for multiple drugs; all 5 drugs have enough R/S in-set. Saves ~2-3 days compute. | SWE |
| 24 | 2026-07-08 | 7 | Per-drug expert rules baselines grounded in determinants present (carbapenemases/AME/QRDR/sul-dfr/AmpC); cefoxitin excludes ESBL, gentamicin excludes aac(6'). | Fair, drug-specific baselines; ⚕ review recommended. | SWE (⚕) |
| 25 | 2026-07-08 | 8 | **FLAG: calibrated XGBoost has high VME (0.23-0.30) at 0.5 threshold on imbalanced drugs**; logreg (balanced) better on VME. Next: VME-bounded threshold selection. | VME is the clinical metric; 0.5 threshold is not clinically optimal. Threshold artifact, not discrimination failure. | SWE |
| 22 | 2026-07-08 | 5 | Lineage split = **StratifiedGroupKFold** (was GroupShuffleSplit). MLST via `mlst` (klebsiella scheme); untypeable → unique synthetic lineage. | Group-only split drifted class balance (test 79R/201S) because resistance is lineage-associated; StratifiedGroupKFold keeps lineages disjoint AND balances R/S (test now 184/184). Zero leakage preserved. | SWE |

| 26 | 2026-07-08 | 8 | **VME≤3% operating point** (config `vme_target`): threshold picked on out-of-fold TRAIN predictions, evaluated on test. Achieved cleanly for cipro/TMP-SMX; meropenem/gentamicin partial; cefoxitin degenerate (data-limited). | User set 3% clinical target. OOF selection (not a small val slice) stabilises thresholds. Honest generalization gap reported, not hidden. | SWE + user |

| 27 | 2026-07-09 | FM | **GNN tested and rejected (honest negative).** Isolate-similarity GCN (AMR-GNN style, Nat.Commun. 2026): each genome a node, kNN edges by feature similarity, transductive node classification. On identical lineage-grouped folds the GCN was WORSE than a plain MLP on the same features for all 5 drugs (GCN−MLP −0.007 to −0.044); robust across kNN k∈{3..50}. | User believed GNN > ESM-2; we tested it properly rather than assume. Reason it loses: our node features are curated AMRFinderPlus determinants (near-deterministic gene→phenotype), so the population-structure graph adds no residual signal and its smoothing blurs the sharp determinant boundary. AMR-GNN's gains came from noisy *unitig* features on complex-mechanism P. aeruginosa. → `summary_20_gnn.md`. | SWE |
| 28 | 2026-07-09 | FM | **ESM-2 150M adopted for meropenem MIC (honest positive).** Extract each AMR protein per genome (AMRFinderPlus coords→translate), embed 1,945 unique alleles once on MPS, mean-pool per genome, add to MIC regressor. Meropenem EA 68.4%→73.2% (+4.8 pts, 30 paired folds, positive in 77%, Wilcoxon p=0.0004) AND variance halved (7.9%→4.7%). Neutral/negative on gentamicin/cipro/cefoxitin. | Allele identity drives carbapenem MIC (KPC-2 vs KPC-3, OXA/NDM/VIM variants, porin mutations) — presence/absence discards it; ESM-2 recovers it. Other drugs' determinants already saturate. Laptop-scale (dedup 58,502→1,945). → `summary_21_esm2_mic.md`. | SWE |

| 29 | 2026-07-10 | FM | **ESM-2 650M gives no gain over 150M — use 150M.** Re-embedded the 1,945 alleles with 650M (1280-d), same 30 paired meropenem folds: determ+650M EA 73.6% vs determ+150M 73.2% — head-to-head Δ +0.4, 650M wins 50% of folds, Wilcoxon p=0.68 (n.s.). | The carbapenem allele distinctions (KPC-2/3, OXA/NDM/VIM family, porin frameshifts) are gross sequence differences the small model already resolves; 650M's extra capacity adds nothing. Practical win: 150M is laptop-scale (minutes on MPS, no 2.5 GB model). → `summary_21_esm2_mic.md` §Scaling. | SWE + user |

| 30 | 2026-07-10 | FM | **ESM-2 extended to A. baumannii carbapenems — replicates the carbapenem win.** Organism-parametrized `esm2_mic.py` (ORGANISMS registry); extracted 1,395 genomes → 648 alleles (586 new vs shared cache). Paired 30-fold CV: **imipenem** 53.8%→60.5% EA (+6.7, p=0.0001, wins 80%); **meropenem** 44.8%→48.4% (+3.6, p=0.051, borderline — n=449/67 lineages, high variance). | Confirms the effect is carbapenem-specific and cross-organism: allele identity of OXA-23/24/58/51 carbapenemases drives A. baumannii carbapenem MIC, which presence/absence discards. Meropenem reported honestly as suggestive-not-significant (data-limited), not overclaimed. → `summary_21_esm2_mic_abaum_150M.md`. | SWE + user |

| 31 | 2026-07-10 | Demo | **Web demo extended to all 4 organisms.** Shared `src/app/registry.py` (org→taxon/amrfinder-string/features/drugs); generalized `save_models.py` (20 models under `results/models/<org>/`), `predict.py`, `report.py`, `streamlit_app.py` (organism dropdown). Removed superseded flat K.pneu models. Streamlit boot-verified. | Was K. pneumoniae-only; other 3 organisms existed only as eval numbers. Now a 4-pathogen live demo across the Gram divide — generalization is shown, not just claimed. Spot-checks biologically correct (mecA→oxacillin R, blaKPC→meropenem R). → `summary_22_multiorganism_demo.md`. | SWE + user |
| 32 | 2026-07-10 | Research | **Deep literature sweep (2 parallel agents) for improvement ideas.** ADOPT (cheap): regulator-grade FDA/CLSI thresholds (VME≤1.5/ME≤3/CA≥90/EA≥90), random-vs-lineage leakage-Δ headline, bootstrap+DeLong CIs, MCC/PPV-NPV, TRIPOD+AI. EXPERIMENT: decision-curve analysis, risk-coverage/AURC for conformal, multi-task low-data drugs (one global lineage split!), extend ESM-2 alleles beyond carbapenems. REJECT (defense material): pan-genome/unitig k-mers (0.93→0.77 val drop = leakage), genome LMs (re-learn lineage), SMOTE (synthesizes genomes), attention-as-replacement (trees+SHAP win on curated determinants). | User asked to not miss boosting ideas. All ranked by impact-per-effort with leakage/honesty checks; keeps scope + honest-science intact. → `summary_23_improvement_research.md`. | SWE |

| 33 | 2026-07-10 | Rigor | **Three cheap-win rigor additions implemented (research #1–3).** (1) `clinical_rigor.py`: FDA/CLSI table — VME/ME/CA vs bars (VME≤1.5/≤3, ME≤3, CA≥90) + EA from MIC, PASS/FAIL flags. (3) same script: bootstrap 95% CIs on VME/ME/CA/AUC + **DeLong** paired test model-vs-rules — **model beats rules on discrimination for all 5 drugs, p from 2e-14 to ~0**. (2) `leakage_delta.py`: random vs lineage-held-out vs temporal AUC — mean inflation **+0.010** (max +0.032 cefoxitin). Makefile targets `rigor`/`leakage`. | Converts existing outputs into regulator-grade, statistically-defensible claims. Honest notes: high ME on meropenem/cefoxitin from VME-safety threshold (shown, not hidden); small leakage-Δ is *evidence determinants encode mechanism not lineage* (validates feature choice), still reported lineage-held-out as headline. → `summary_24_clinical_rigor.md`, `summary_25_leakage_delta.md`. | SWE |

| 34 | 2026-07-10 | Demo | **Robust genome upload + provider-flexible AI narrative.** Upload: gzip detected by MAGIC BYTES not extension (mis-named .gz/.fna both work), FASTA sanity-check with friendly errors, case-insensitive; all 4 formats (.fna/.fasta/.fa/.gz) verified incl. live AMRFinderPlus run. Narrative layer now supports **Claude / DeepSeek / Gemini** (OpenAI-compatible HTTP for the latter two), auto-picks by whichever key is set (`ANTHROPIC_/DEEPSEEK_/GEMINI_API_KEY`), `AMR_LLM_PROVIDER` to force; graceful templated fallback with no key. | User has no Claude key and asked about DeepSeek/Google; and asked what uploads show. Narrative is explanation-only so provider is scientifically interchangeable. Demo fully works with zero keys. Anthropic path keeps the SDK; DeepSeek/Gemini via their native OpenAI-compatible endpoints. | SWE + user |

| 35 | 2026-07-10 | Demo | **`.env` auto-loading for API keys.** Zero-dependency loader in `registry.py` (`load_dotenv()`, runs at import) reads project-root `.env`; `os.environ.setdefault` so a real shell export still wins. Added `.env.example` template; `.env` git-ignored. | User's `export` didn't persist across terminals (each shell separate). `.env` removes that friction — key lives in one git-ignored file, loaded regardless of terminal. Verified: auto-load picks provider; shell export precedence correct. | SWE + user |

| 36 | 2026-07-10 | Demo | **Gemini narrative fixed — native endpoint + `gemini-flash-latest`.** User's Gemini key (AI-Studio `AQ.`-type, 53 chars) is valid but (a) `gemini-2.5-flash` is "no longer available to new users" (404) and (b) the OpenAI-compat layer rejected the key type. Fix: route Gemini through the **native** `generateContent` endpoint (`x-goog-api-key`) and default to the moving alias **`gemini-flash-latest`**; maxOutputTokens 4096 (flash models spend output on reasoning). DeepSeek stays OpenAI-compat. Verified end-to-end: correct clinical narrative generated. Also note: user's DeepSeek key returned 402 (no account balance). | Robust across key types + won't go stale as models rotate. Diagnosed via models-list (200) then per-model probe. → decision #34/#35 chain. | SWE + user |

| 37 | 2026-07-10 | Deliver | **Reproducibility + write-up + poster completed.** (1) `environment.yml` now pins torch/fair-esm/biopython/scipy (ESM-2 pipeline reproducible). (2) `data/manifest.md` refreshed — all 4 organisms (counts, taxa, AMRFinderPlus/MLST schemes, queries) + ESM-2 derived caches + tool versions. (3) `REPORT.md` folded in §4.4 (DeLong/CIs/leakage), §5.4a-b (ESM-2 + GNN verdict), 4-organism demo, updated repro. (4) **Poster** `results/poster/poster.html` (A0, print-to-PDF, real figures + numbers). (6) Tests added: `test_features.py` (parse_determinants + encode_matrix, refactored a pure `encode_matrix` into build.py) + `test_data_join.py` (join integrity) — **13 passing**. | Closes reproducibility gaps + Week-4 deliverables. Item #5 (co-builder biological ⚕ sign-off) remains human. → REPORT.md, manifest, poster. | SWE |

| 38 | 2026-07-10 | Demo | **Wrong-organism / non-bacterial input guard.** User uploaded a *fungal* protein FASTA (Harposporium, 7,873 proteins); AMRFinderPlus found **0 AMR determinants** (correct) but the model still emitted calls — cefoxitin flipped to RESISTANT (P=0.16) because its low VME-safety threshold trips on an all-zeros vector (cefoxitin resistance = porin *loss*/absence). Fix: `build_findings` returns `n_in_vocab`; web app + CLI **withhold predictions and warn** when 0 in-vocabulary determinants are found (a real target strain always carries intrinsic determinants, so 0 ⇒ wrong species/not a genome). Verified: fungus → warning; real K. pneumoniae (15 dets) → predicts normally. | Models are a function of the determinant vector and will "predict" on any input; 0-determinant guard catches non-target/non-bacterial uploads. Honest limitation noted: a *wrong bacterium* with overlapping determinants still isn't taxonomically validated. | SWE + user |

| 39 | 2026-07-10 | Demo | **MLST species verification (stronger wrong-organism guard).** On upload, run `mlst` (cached for known genome_ids); a scheme is species-specific. `species_verdict` is **conservative**: hard-withhold only when the genome types to a *different panel organism's* scheme (klebsiella/abaumannii/saureus/ecoli); ambiguous/unmapped schemes (e.g. an E. coli typing to the Citrobacter `cfreundii` scheme — a known Enterobacterales MLST quirk) → soft "unverified" note, no false block. Protein `.faa` → unverified (mlst needs nucleotide). Verified: A. baumannii genome as kpneu → **mismatch, withheld** (and it had 1 in-vocab determinant, so the 0-determinant guard alone would have missed it). Registry gained per-organism `schemes` + `SCHEME_ORG`. | Closes the wrong-bacterium gap #38 flagged. Honest limit: E. coli vs close Enterobacterales may read 'unverified' not 'confirmed'; cross-genus/cross-Gram mismatches are caught reliably. | SWE + user |

| 40 | 2026-07-10 | Demo | **Intrinsic-marker species check (protein-safe) added to MLST.** Data-driven signature genes per organism (kpneu fosA/oqxA/oqxB ~95%; abaumannii blaADC ~99%; saureus blaZ/blaI/blaR1 ~90%; ecoli weak: uhpT_E350Q/gyrA_S83L — all 0% in other panel orgs). `species_verdict(org,scheme,dets)` combines both: a mismatch from EITHER MLST or another org's markers withholds. Key win: catches wrong-species on **protein `.faa`** input (MLST is nucleotide-only) — e.g. K. pneumoniae proteins uploaded as E. coli → mismatch via fosA/oqxA. Verified across 6 scenarios. | User asked to strengthen E. coli / add protein-capable check. Honest residual: E. coli genomes typing to `cfreundii` with no E.coli-specific markers stay 'unverified' (proceed+note) — no false block. **NOTE: user asked to add *Harposporium* (a FUNGUS) as an organism — declined: no antibiotic phenotype, no AMR genes, no MLST, not in scope; bacterial organisms can be added via the ORGANISMS registry + BV-BRC training data.** | SWE + user |

| 41 | 2026-07-10 | Scope | **Round-4 expansion scaffolded (4→10 organisms).** Added 6 candidates — P. aeruginosa (287), S. enterica (28901), E. faecium (1352), E. cloacae (550), S. pneumoniae (1313), C. jejuni (197) — to `ORG_ACQ`, `ORGANISMS`, `RULES` (+ new drugs: ceftazidime/tobramycin/chloramphenicol/tetracycline/vancomycin/penicillin). Tooling verified (AMRFinderPlus `--organism` + MLST scheme all exist); config consistency checked. Plan in `docs/round4_expansion_plan.md`. **User chose:** scaffold-now → acquire P. aeruginosa first, ~3,000+ genomes/org, one at a time with approval gate. Panels PROVISIONAL pending BV-BRC count validation + ⚕. | Chitinophaga (bacterium but environmental, no AMR phenotype data/AMRFinderPlus/MLST) and Harposporium (fungus) both correctly excluded. **Acquisition BLOCKED: BV-BRC returning 503 (outage) at scaffold time; flagged heavy compute per project rules — not started.** | SWE + user |

| 42 | 2026-07-11 | Organism | **P. aeruginosa added (organism #5, first Round-4).** 1,160 genomes→1,057 QC→4-drug panel (amikacin dropped, 126R). Unseen-lineage ROC 0.84–0.98; ML adds most on ceftazidime (+0.17) & meropenem (+0.10, regulatory resistance), matches rules on cipro/tobramycin. **Caught+fixed a baseline artifact:** provisional rules had intrinsic-universal genes (blaPDC 100%, aph(3')-IIb 99%) → fake rulesROC 0.500; removed → honest 0.702/0.971 (reported the corrected, not the inflated gap). **Fixed fosA marker collision** (99% in both kpneu & PA → dropped from kpneu markers). Mined PA markers aph(3')-IIb/catB7/nalC_G71E. In demo (5 orgs) + species check; 13 tests pass. → `summary_26_paeruginosa.md`. | Honest-science: corrected artifact before reporting; ⚕ flag on PA rule genes. Next: Salmonella. | SWE + user |

| 43 | 2026-07-13 | Organism | **Salmonella enterica added (organism #6).** 3,109 genomes→2,321 QC→5 drugs. ceftriaxone 0.996, cipro 0.978, ampicillin 0.947 (ML≈rules); **TMP-SMX 0.832 ML adds** (vs 0.565 weak sul/dfr baseline, genuine not artifact); **chloramphenicol honest weak spot** (ML 0.880 < rules 0.918, VME 10.9% misses target). No intrinsic-gene artifact; no reliable species marker (MLST-based). **Fixed demo bugs:** 0-determinant guard now only rejects for organisms WITH intrinsic markers (susceptible E.coli/Salmonella no longer falsely rejected); imbalanced-drug over-calling note points to P(resistant). → `summary_27_senterica.md`. | SWE + user |
| 44 | 2026-07-13 | Organism | **E. faecium added (organism #7, VRE).** 2,285 genomes (102 dl-fails)→1,890 QC→3 drugs (cipro/erythromycin dropped, too few). vancomycin **0.998** (van=mechanism); ampicillin **0.992 — ML captures pbp5** (92% in features) that the β-lactamase baseline (0.500, wrong mechanism) misses — verified genuine, cleanest ML-adds case; tetracycline 0.911. Markers msr(C)/pbp5_N496K. **Recurring ⚕ flag: rules baselines are organism-agnostic but ampicillin/penicillin mechanisms differ by organism** (β-lactamase vs pbp5) → needs organism-specific rule genes (same class as P. aeruginosa intrinsic-gene fix). → `summary_28_efaecium.md`. | SWE + user |

| 45 | 2026-07-14 | Rigor | **Organism-specific rule baselines (`ORG_RULES`).** The known-gene baseline now varies by (organism, drug) where the mechanism differs — `rules_for(org, drug)` overrides the shared `RULES`, wired into `organism_pipeline` eval + `save_models` bundles. First override `(efaecium, ampicillin)→pbp5` (not β-lactamases): E. faecium ampicillin rules baseline **0.500→0.638**, so ML's 0.992 is now measured vs an honest mechanism-based baseline (+0.35, not a strawman). `(spneumoniae, penicillin)→PBP1a/2b/2x` pre-registered. | Closes the recurring #42/#44 issue (ampicillin=β-lactamase in Enterobacterales but pbp5 in Enterococcus). Makes the ML-vs-rules comparison fair per organism. 13 tests pass. → `summary_28_efaecium.md`. | SWE + user |

| 46 | 2026-07-14 | Organism | **E. cloacae SKIPPED — insufficient lab AST data (data-driven).** Checked all 3 plausible taxa: species 550 (≤109/drug, all <150/class), E. cloacae complex 354276 (~0), genus Enterobacter 547 (0). Not enough to train honestly → dropped per "drop rather than overclaim". **S. pneumoniae started instead** (taxon 1313, well-populated: 15,511 genomes; panel penicillin/erythromycin/tetracycline/TMP-SMX — fluoroquinolones dropped, pneumococcal FQ-R rare, ≤3 R). Note: penicillin uses the pre-registered `ORG_RULES` PBP1a/2b/2x baseline (#45). | Honest data-driven organism selection; don't fabricate a model on ~100 genomes. Remaining queue after S. pneumoniae: C. jejuni. | SWE + user |

| 47 | 2026-07-15 | Bug | **Truncation-floor bug fixed (small-genome organisms).** `download_genome` used a fixed 2.5 MB "truncated" floor (K. pneumoniae ~5.5 Mbp) that falsely rejected valid S. pneumoniae (~2.1 Mbp) — and would have entirely blocked C. jejuni (~1.7 Mbp). Now scales per organism (`min_mbp*0.55`, passed from `ORGANISMS[org]["len"]`). Verified: catches real 0.2 MB truncations, passes valid 2 MB genomes. | Caught mid-download (S. pneumoniae skipping everything). Small-genome organisms now acquire correctly. | SWE |
| 48 | 2026-07-15 | Organism | **S. pneumoniae added (organism #8).** 3,877→3,857 QC→4 drugs (FQ dropped, R rare). penicillin **0.961** (pre-registered pbp `ORG_RULES` baseline 0.860 works — #45 paid off; drivers pbp2b/pbp2x); erythromycin 0.983, tetracycline 0.969 (ME 0.89 over-call). **TMP-SMX honest scope limit:** causal folA/folP not in AMRFinderPlus catalog (0% in features), so rules 0.500 AND ML 0.858 is **co-selection-driven**, not mechanistic — reported as such, not as an ML-adds headline. No species marker (MLST-based). → `summary_29_spneumoniae.md`. | SWE + user |

| 49 | 2026-07-15 | Organism | **C. jejuni EXCLUDED — expansion concludes at 8 organisms (user call).** Only ~489 lab-phenotyped genomes; a single drug (tetracycline 336R/153S) barely clears ≥150/class, all others ≤96 R. Below the bar held for every other organism → excluded like E. cloacae (data-limited). **Round-4 expansion COMPLETE:** added P. aeruginosa, Salmonella, E. faecium, S. pneumoniae (4→8 organisms); E. cloacae + C. jejuni excluded for insufficient public lab data. | Honest data-driven stop: 8 organisms all have solid multi-drug panels; don't add a thin 1-drug organism for a bigger number. | SWE + user |

| 50 | 2026-07-17 | Demo | **LLM narrative retries transient errors.** Gemini free tier returned a 503 (Service Unavailable) — the app correctly fell back to the templated report, but now `llm_narrative` retries transient errors (503/502/500/429/timeout/overloaded) 3× with backoff (2s,4s) before falling back; non-transient (401/404) still fail fast. Added `import time`. Verified: narrative generates when Gemini is up; graceful template fallback otherwise. | Not a code bug — Google-side outage; retry auto-recovers so the user doesn't have to re-click. | SWE + user |

| 51 | 2026-07-17 | Audit | **End-to-end audit (3 reviewers) + Critical/High fixes.** Science core verified sound (zero leakage, DeLong, ESM-2, species check all correct). Fixed: (1) **calibration non-negotiable #7** — `clinical_rigor.py` generalized to all 8 organisms (isotonic-calibrated XGBoost + **Brier**), and `organism_pipeline` now reports calibrated XGBoost+Brier; regenerated all metrics. (2) **lineage-clustered block bootstrap** (was i.i.d. — too narrow). (3) caught+fixed a **self-introduced bug**: AUC/DeLong were on isotonic-calibrated scores (tie-deflated); now on RAW scores, Brier on calibrated. (4) persisted per-organism `<tag>_split.csv` + **parametrized the leakage test over all 8** (20 tests pass). (5) fixed broken `make eval` (missing module → clinical_rigor). (6) manifest +4 organisms. (7) de-staled README/REPORT/poster/docstrings 4→8. (8) new **`generalization_heatmap.png`** (8 orgs × 18 drugs) + extended cipro transfer matrix 4→6 organisms. | Expansion had outrun rigor/docs; now internally consistent and honest across all 8. Honest new findings surfaced (Salmonella cipro/chloramphenicol: rules ≥ ML; A. baumannii carbapenems rules ≥ ML) — reported, not hidden. → `summary_24_clinical_rigor.md`. | SWE + user |

| 52 | 2026-07-17 | Rigor | **Fixed the remaining real correctness/honesty issues (audit Medium tier).** (1) **Censored-MIC EA bias**: `>32`→32 sign-stripping distorted Essential Agreement — now censor-aware (right/left-censored MICs scored as bounds, not boundary values) + full 5-fold CV. Turns out **76–86% of K. pneumoniae MICs are censored**, so this mattered: meropenem EA 59%→75% (old scoring *under*-counted correct high predictions on `>32`). (2) **Conformal over-claim**: "statistically-guaranteed" → "empirically-validated per-class coverage under lineage shift" (exchangeability is deliberately broken; we measure coverage, not assume it) — docstring + REPORT + poster. (3) **In-sample threshold** in `train_panel` calibrate path: was re-picked on the same slice the isotonic was fit on; now keeps the honest OOF threshold (calibration is monotone → operating point unchanged; calibrated scores used only for Brier). Regenerated panel + MIC metrics. | Genuine methodological fixes, not cosmetic. 20 tests pass. → `summary_19_mic_regression.md`, `summary_09_conformal.md`. | SWE + user |

| 53 | 2026-07-18 | Rigor | **Three net-new analyses added (audit improvement tier).** (1) **MCC + PPV/NPV** (at 10/30/50% prevalence) added to the all-8 regulator table (`clinical_rigor.py`) — robust to the class imbalance that inflates accuracy/CA. (2) **Decision-curve analysis** (`decision_curve.py`, `summary_30`, fig) — standardized net benefit vs threshold probability; model beats rules + treat-all across the low-p_t (VME-averse) region for most K. pneumoniae drugs (cipro only >0.33, honest). (3) **Risk-coverage + AURC** (`risk_coverage.py`, `summary_31`, fig) — deferring least-confident ~30% halves error/VME (meropenem VME 8.7%→3.5%); AURC 0.013–0.056. `clinical_rigor` now saves `pooled_predictions.pkl` (reused by both). Makefile `dca`/`riskcov` targets; REPORT §5.6–5.8. | Adds the clinical-utility + rigorous-abstention axes most AMR-ML omits; all honesty-safe (post-hoc on lineage-held-out predictions). 20 tests pass. | SWE + user |

| 54 | 2026-08-22 | Rigor | **Submission-polish pass + numbers audit + TRIPOD+AI appendix.** (1) **Caught 4 stale/unsupported numbers** by re-deriving every headline claim from `results/metrics/*.json`: the abstract claimed cefoxitin "lifts ROC-AUC to 0.96" (**no model reaches that** — 0.906 logreg / 0.935 calibrated XGB) and a "0.88–0.98" range (actual 0.91–0.98); the cefoxitin rules baseline misses **96.6%** of resistant strains (368/381), not the "~92%" repeated in poster + both decks + speaker guide; §4.3 cited "AUC 0.899 vs rules 0.518" (actual 0.905 vs 0.500) and "DeLong p from 2×10⁻¹⁴" (actual worst p = 5.3×10⁻²⁴); the **poster table was a whole generation stale** (pre-#51 regeneration). All corrected from source JSON, everywhere. (2) **VME honesty upgrade:** "VME clears ≤3% on all five" → point estimates do, but lineage-clustered CIs are wide and **meropenem's upper bound is 3.49%**, so we now claim the target is met *in expectation, not guaranteed at the upper bound* — in REPORT, both decks, and speaker notes. (3) **Cross-species claim de-staled:** "transfers across the three Gram-negatives (0.74–0.98)" was computed at 4 organisms and is false at 6 (Salmonella→P. aeruginosa is **0.393**); replaced with a tiered table — near-lossless within Enterobacterales (0.95–0.98), degraded to non-fermenters (0.39–0.89), collapsed across the Gram divide (0.46–0.69). (4) **Appendix A: TRIPOD+AI checklist** (all 52 rows, verified against the official PDF + Europe PMC + the E&E supplement) mapped honestly — ✅30 / ⚠️10 / ❌1 / ➖11, with the ❌ (no sample-size calculation) and the N/A evaluation track both stated plainly. (5) **Appendix B: next steps.** (6) **References rebuilt** — 20 entries with DOIs resolved against Crossref/PubMed/doi.org; caught that **Nguyen 2019 is *Salmonella*, not *Klebsiella*** (the K. pneumoniae paper is Nguyen 2018, Sci Rep) and cited both; standards documents marked "no DOI" rather than given invented ones. (7) Report structure fixed (4.1→4.4 and 5.1→5.7 now sequential, heading levels nested, complete figure index); stale "🔜 remaining poster" and "50 decisions" markers removed. PDFs regenerated (51 pp each, verified). | Every fix moved a claim **toward** what the data support — two of them made the result look *better* (96.6% miss rate, p = 5×10⁻²⁴), one made it look worse but honest (the VME CI), which is the point. 20 tests pass. | SWE |

| 55 | 2026-08-22 | Validation | **External validation Step 1 — cohort scoped, metadata only (179 MB), GO.** Caught the decisive trap first: **BV-BRC ingests AMR phenotypes from NCBI BioSample/Antibiogram**, so NCBI Pathogen Detection is *upstream* of our training data and a naive external validation would have been **testing on training isolates**. Removed overlap by BioSample/assembly **anti-join**: of 1,630 NCBI PD *K. pneumoniae* isolates carrying ≥1 panel drug, **262 (16.1%) overlapped and were excluded**, leaving **1,368 unseen** — 1,038 with downloadable assemblies, 496–780 usable R/S per drug, balance 0.33–0.50 (better than training on several drugs), collection years 2013–2023 (mode 2022–23, later than our pull). AST is the bottleneck, not genomes: only 2,529/161,779 (1.6%) of NCBI PD K. pneumoniae carry any AST. **Pre-registered the Step-2 analysis plan before downloading a single genome** — frozen models, no retraining/re-thresholding, all five drugs reported whatever the result, rules baseline scored on the same cohort, internal-vs-external shown side by side. Disclosed three caveats up front: 129 training genomes (3.4%) lack accessions → ≤12.4% undetected-overlap upper bound; this is **independent-source, not independent-population** (shared submitter ecosystem); NCBI does not vet submitted AST methods, so label noise biases the external result *downward*. → `summary_32_external_validation_scoping.md`. | Spending 179 MB to answer "is there enough genuinely independent data?" before committing ~5.1 GB + ~14 h of annotation. Pre-registration makes the result impossible to cherry-pick — the point of the exercise is that the number is honest, not that it is high. | SWE + user |

| 56 | 2026-08-22 | Validation | **External validation Step 1 revised — second source added, and a real gap in the first pass caught.** A parallel literature/source sweep surfaced the **EMBL-EBI AMR Portal (CABBAGE)** (Dickens et al. bioRxiv 2025, CC BY 4.0, no login), which we verified live (md5-checked, 16.6 MB). It is **strictly better than NCBI PD on independence** because it carries a **`database` provenance column** — of 133,136 K. pneumoniae phenotype rows, 55,263 are `PATRIC` and ~47k more are PATRIC-combined, so BV-BRC-touched rows are excluded **by construction** rather than by hoping accessions collide. After PATRIC-filter + anti-join: 800 isolates; **union with the NCBI route = 1,312 (1,144 with usable panel labels)**, per-drug 506–944 R/S, balance 0.33–0.50. Bonus: the two sources overlap on 524 isolates and produced **zero conflicting R/S calls** — independent evidence that the phenotype labels are stable across curation pipelines. **Caught a genuine methodological gap in the first pass:** the anti-join is *identity-level* and does **not** remove **near-clonal siblings** (an external ST258 isolate is a different BioSample but the same lineage the model already learned) — i.e. non-negotiable #1 reappearing at the external boundary. Step 2 plan amended to **require `mlst` and to report two numbers: all external isolates, and only STs absent from training**. Also pinned down that **features must be recomputed, not reused** — NCBI ships refgene DB `2026-01-21.1` vs our `2026-05-15.1`, and the Portal discloses no DB version; only 266/688 of our feature columns appear in its vocabulary, so reusing precomputed calls would depress performance for reasons unrelated to generalization. Logged a **false-external-validation blacklist** (EuSCAPE, both Nguyen papers, Moradigaravand, Gordon/Bradley, Pathogenwatch, AMR_benchmarking, AllTheBacteria) — all already inside BV-BRC or genotype-predicted. Revised cost: **~1.8 GB + ~15 h**. → `summary_32`. | Better independence guarantee, +43% cohort, and a leakage hole closed *before* producing a number rather than after a judge found it. The lineage point is the important one: identity de-dup would have let us publish an "external" result that quietly re-tested known lineages. | SWE |

| 57 | 2026-08-22 | Rigor | **Documentation-integrity audit — found and fixed a REAL BUG in the shipped cefoxitin baseline.** Systematically checked every shipped model's rules baseline by asking "does this gene list ever fire in this organism's own genomes?". Two never-fired. One is a **known, documented biological limit** (S. pneumoniae TMP-SMX — causal folA/folP absent from the AMRFinderPlus catalog, already disclosed in #48 and REPORT §4.2). The other was a **genuine bug**: `rules_for("kpneu","cefoxitin")` fell through to the shared `RULES["cefoxitin"] = ["mecA","mecC"]`, which is the ***S. aureus*** mecA screen (cefoxitin is the standard MRSA surrogate test) — meaningless in *Klebsiella*, where mecA/mecC occur in **0 of 3,850** genomes. The baseline therefore never fired, scoring a degenerate **0.500 by predicting a constant**, and that artifact propagated into `clinical_rigor.json`, REPORT §4.3, the poster, both decks, the speaker guide and both walkthroughs. `train_panel.py` had always used the correct plasmid-AmpC list, which is why `panel_metrics.json` showed an honest 0.517 — the two sources silently disagreed. **Fix:** added `("kpneu","cefoxitin"): ["blaCMY","blaDHA","blaACT","blaFOX","blaMOX"]` to `ORG_RULES` (S. aureus correctly keeps mecA/mecC); re-saved the kpneu bundles; re-ran `clinical_rigor --organism kpneu`. Corrected baseline: **ROC 0.518**, fires on 57 genomes, misses **1,469/1,525 (96.3%)** with 1 false positive. **The headline claim survives and is now honestly supported** — the AmpC lookup genuinely fails on cefoxitin, rather than failing because we handed it the wrong organism's genes. Also standardised every narrative cefoxitin figure on the pooled CV (0.518 / 96.3% / 0.905) and added a note to REPORT §4 distinguishing the single-split table from the pooled-CV table so 0.517 vs 0.518 does not read as an error. | Caught by a "does this baseline ever fire?" check that should have existed from the start — the same class of artifact as #42 (P. aeruginosa intrinsic genes). Reported the corrected number, not the flattering one: 0.500 made the baseline look worse than it is. | SWE |

| 58 | 2026-08-22 | Validation | **External validation COMPLETE — the models degraded substantially, and that is the reported headline.** Ran the plan pre-registered in #56 exactly: 1,143 of 1,144 independently-curated *K. pneumoniae* isolates (one withdrawn from NCBI), features **recomputed** with our own AMRFinderPlus 4.2.7 / DB 2026-05-15.1, models applied **frozen** — no retraining, no re-tuning, no threshold re-selection. **Result: ROC-AUC falls from 0.905–0.983 internal to 0.795–0.835 external, and cefoxitin collapses 0.905 → 0.596.** External VME is 5.2–15.9%, so **the VME≤3% operating point does not transfer at all**. **What survives:** ML still beats the gene lookup externally on **4 of 5 drugs** in both cohorts (+0.027 to +0.092). **What does not:** the cefoxitin headline is now **internal-only** — externally the gap is +0.027, and on lineages absent from training the **rules baseline beats the model** (0.620 vs 0.592). **Ruled out a feature-extraction artifact before concluding** (this pattern is exactly what a broken pipeline produces): external genomes carry 14.4 determinants vs 15.1 in training, 14.2 in-vocabulary, and **zero** external genomes have no known determinants. Two real causes: (i) **resistance-prevalence shift** — cipro −24 pts, cefoxitin −27 pts — partly self-inflicted by the Phase-2b resistant-enriched top-up, which explains the ME inflation but not the threshold-free AUC drop; (ii) **co-selection does not transfer** — cefoxitin was the drug most dependent on co-carried MDR markers (§5.1) and is the drug that collapses, while the mechanistic AmpC lookup barely moves (0.518 → 0.569). That is the project's clearest evidence that co-selection inflated an internal number, and it was written down as a limitation *before* the test. REPORT abstract, §4.4 and §7 rewritten around this; TRIPOD evaluation track moves from N/A to reported (✅30→33, ➖11→8). → `summary_33_external_validation.md`. | The point of pre-registration is that a bad result gets published too. A measured generalization gap on a lineage-disjoint external cohort is a more useful contribution than another internal AUC — almost no comparable study reports one. | SWE |

| 59 | 2026-08-22 | Audit | **Integrity audit — found and fixed a regression I had introduced, plus an unlabelled claim.** Traced every AUC-like number in every doc back to a metrics file. Two real problems. (1) **I destroyed data:** re-running `clinical_rigor --organism kpneu` after the #57 fix **overwrote** the whole file instead of merging, deleting the regulator-grade metrics (VME/ME/CA/MCC/Brier/CIs/DeLong) for the **other 7 organisms** — 1,695 lines gone from `clinical_rigor.json` and 7 sections from `summary_24`, committed and pushed without noticing. Regenerated all 8 (36 organism-drug pairs restored; kpneu cefoxitin rules correctly 0.5179). **Lesson: a per-organism flag that rewrites a whole-corpus artifact is a footgun — verify file scope after any partial regeneration.** (2) **The "ROC 0.84–0.998 across 8 organisms" claim was unlabelled:** true for the **single held-out split** (min 0.8409, P. aeruginosa meropenem) but **false** for the pooled lineage-grouped CV, where the floor is **0.7507** (P. aeruginosa ceftazidime — regulatory, not gene-encoded resistance). Same two-estimator ambiguity as cefoxitin 0.517 vs 0.518. Every occurrence now names its evaluation and quotes the conservative pooled range alongside. **Also verified, and clean:** no synthetic genomes or labels anywhere (label values are only Resistant/Susceptible/blank — no imputation); 8/8 sampled genome IDs back to real 1.6–1.7 MB downloaded FASTA + real AMRFinderPlus output; 28 GB of genuinely downloaded raw data; all `np.random` uses are seeded bootstrap/CV, not data generation. | The question "did we hallucinate anything?" deserved a check, not a reassurance. Nothing was invented — but two real defects surfaced, one of them self-inflicted an hour earlier. | SWE |

| 60 | 2026-08-22 | Audit | **Full-project integrity re-audit — found 1 severe metric bug, 1 train/test contamination, 1 self-contradiction, and 3 misstatements of published work.** Independently re-implemented the core evaluation from raw AMRFinderPlus TSVs without importing project code: **AUC reproduces to ±0.003 and VME to ~0.6 pts** under the same nested-CV protocol, confirming the headline discrimination numbers are real. Then: **(1) SEVERE — Essential Agreement was inflated 12.6–26.7 pts.** `_parse_mic` collapsed strict `>` into non-strict `>=` and scored both with the `>=` rule; on a doubling panel `>2` means true MIC ≥ 4, so agreement needs pred ≥ 2, not ≥ 1. Strict `>` is the **largest category** (40.5% of ciprofloxacin measurements, verified against BV-BRC directly). Corrected EA: cipro **92.7 → 66.0**, gentamicin 85.1 → 71.9, meropenem 75.4 → 62.7, cefoxitin 79.9 → 62.2. **No drug now clears the ≥90% CLSI bar** — ciprofloxacin was the only one that did. **(2) train/test contamination in the flagship figure:** `figures.py` picked the operating threshold on the *test* fold and scored it on that same fold, so the plotted VME bars met the 3% target *by construction*; 3 of 5 drugs breach it under an honest train-OOF threshold. Fixed. **(3) self-contradiction:** `risk_coverage.py` hardcoded 0.5 instead of the VME-capped operating point, so two shipped reports gave VME figures differing by up to **28×** for the same predictions (cefoxitin 19.4% vs 0.7%). Fixed; now 0.4% vs 0.7%. **(4) misstated published work:** §4.1 quoted Nguyen et al. 2018 as "ROC-AUC" when that paper reports **MIC essential agreement and no AUC at all**, and got 2 of 3 values wrong (gentamicin 0.95 not 0.91; cipro 0.98 not 0.96); the whole cross-study comparison is **withdrawn**. **(5)** The gold "published band" (0.85–0.96) on the benchmark figure was uncited, author-chosen, and plotted on a ROC axis over MIC-accuracy numbers — **removed**. **(6)** "k-mer models lose 0.1–0.2 AUC" had **no source** and is withdrawn from all 6 files; replaced with the citable Moradigaravand ST131 comparison. **(7)** "we implemented the SOTA graph model" overstated a 2-layer GCN vs AMR-GNN's multi-graph fusion — scoped to "AMR-GNN-style". **(8)** `leakage_delta` encoded undated genomes as year −1, dumping them all into the "past" arm (39–56% of it); now excluded. **Verified clean:** no synthetic genomes or labels, VME/ME denominators correct, bootstrap CIs genuinely lineage-clustered, calibration/threshold selection leak-free in the metrics path, raw-vs-calibrated score discipline correct. | The user asked whether anything was hallucinated. Nothing was invented from nothing — but a metric bug inflated a headline pass to a fail, a figure was circular, and three statements about other researchers' results were wrong. Found by re-deriving rather than re-reading. | SWE |

## Open decisions

**Genuinely open — needs microbiology sign-off (1):**
- ⚕ **Phase 3 — "Intermediate" handling.** Isolates phenotyped `I` are currently **excluded**; models
  are trained and evaluated on `R`/`S` only. This is the standard choice in the genomic-AMR literature
  and keeps the VME/ME definitions clean (both are defined against a binary reference), but it
  (a) discards data and (b) means the tool is silent on exactly the isolates a clinician finds hardest.
  The alternatives are to fold `I` into `R` (conservative, treatment-relevant, inflates apparent
  resistance prevalence) or to model three classes (loses the CLSI error vocabulary). **Deliberately
  left to the microbiologist** — it is a clinical-interpretation call, not an engineering one. Any
  change requires re-running the full panel. **Status: open, documented, and disclosed in the report.**

**Closed by what shipped (2) — recorded here because they were previously listed as open:**
- ✅ **Phase 2b — QC thresholds on the full panel.** Resolved by the Phase-8 refresh (#`summary_08`):
  the identical QC gate (completeness ≥90%, contamination ≤5%, contigs ≤500, length 4.5–7.5 Mbp) was
  **re-run on the enlarged 3,850-genome set**, not carried over. It dropped 66 genomes (~1.7%) and
  remained class-balanced, so the distribution shift the item worried about did not materialise and
  the thresholds stand unchanged. No further action.
- ✅ **Phase 6/7 — XGBoost hyperparameter scope.** Resolved as **"one fixed, documented configuration,
  no per-drug tuning"**: `n_estimators=300, max_depth=4, learning_rate=0.1, subsample=0.9,
  colsample_bytree=0.8, scale_pos_weight=<class ratio>, random_state=42`, isotonic-calibrated, used
  identically for every drug and every organism (`train_panel.py`, `save_models.py`,
  `organism_pipeline.py`, `conformal.py`). This was a deliberate honesty choice, not an oversight:
  per-drug tuning inside a lineage-grouped CV, on top of a VME-constrained operating point selected
  from the same folds, is a realistic route to optimistic bias on the small resistant class — and no
  tuning gain was ever demonstrated to justify that risk. The fixed configuration is therefore part of
  the reported method, and the honest framing is "untuned defaults, tuned threshold."

### Resolved
- ✅ Organism + drugs (#11–13) · lab-only labels (#8) · thresholds (#9) · QC (#19) · annotator=AMRFinderPlus (#20) · lineage method=MLST (#22).
